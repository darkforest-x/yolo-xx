# yolo-xx 协作约定

## 汇报口径（owner 要求，2026-08-04）

**给数字，不下判决。** 结论由 owner 来下。

不要写这类话：

- 「这个模型失败了」「这条路走不通」「我不乐观」「期望收益很低」
- 「盲目再训一版没意义」「建议放弃」
- 任何对整条路线的定性宣判

要这样写：

- 「连续扫描精度 3.9%，上一版 5.5%」——报数字和对比基准，句号
- 「这一版没有超过基准。下一个可试的变量是 X」——描述这一次的结果，然后给下一步
- 结果不理想时，重点放在**下一个可以动的变量**上，而不是这一次有多差

**但数字不许粉饰。** 3.9% 就是 3.9%，不能说成「有所改善」或藏起来不提。
样本内和样本外要分开报，小样本要标注笔数。发现测量方法有问题（比如
考卷难度不同、重复计数、样本外失效）必须直接指出——那是在保护结论质量，
不是唱衰。

**不要长篇解释。** owner 要分析结果时，写 HTML 报告发 TG 群，不要在对话里
铺一大段文字。对话里只说：做完了、关键数字、下一步。

## 项目背景

检测「均线密集」形态用于做空。核心链路：小周期（2m/3m/5m）出信号 →
15m/30m 进场。小周期的价值是同一形态完成得更快（2m 约 25 分钟 vs 15m 约 3 小时），
所以「事后信号」在物理时间上仍然早。

已确认：owner 手标的 1313 个框在 val 期做空 PF 8.98、胜率 79%；在完全相同语境下
随机进场只赢 32%。owner 的判别力是真实的。

## 关键约定

- 阈值冻结 `conf=0.30 / iou=0.70`，不按结果调
- 回测口径：做空、TP −5ATR / SL +2ATR、maker 0.06% round-trip、
  同币种同时只持一仓（不去重会把同一波行情重复计数，实测能把 PF 从 0.96 虚抬到 1.56）
- `available_at` = 输入窗口最后一根 K 线的收盘时间，**不是**框的右边界
- holdout（2026-05-04 之后）双向闸门：`cutoff > 该日` ⟺ manifest 声明
  `holdout_read: true`，两边互相强制，产物永久带印章
- 评估要看**连续扫描精度**，不能只看 1:1 配对集上的 mAP（那个系统性虚高）
- **正负样本必须在「除标签之外的一切维度」上同分布**——渲染管线、采集批次、
  时间段、来源。做不到就**按 source 分层各报一次**：子集全线塌陷 = 来源混杂，
  各子集一致 = 真信号。合池前先看各来源的正类率差，差得越大越要怀疑模型在分来源。
  分组 CV 只防「同一实体跨折」，防不了「不同来源基率不同」——是两种病。
  （2026-08-06：659 样本合池 AUC 0.8067 看似突破，按 source 分层后每个子集只剩
  0.57–0.65；三来源正类率 85%/84%/29%。当时渲染管线已统一，仍然中招——这是 v15
  「正负样本两条渲染管线」的同族变种，触发面从渲染换成了批次。见
  `fable-trading/docs/learnings/pooled-auc-can-be-source-discrimination.md`）

## 环境

- 3060：`zzc@192.168.1.2`（同一 WiFi，IP 会变），`C:/fable/.venv/Scripts/python.exe`
- 本机 venv：`.venv`，ultralytics 8.4.89 + torch 2.8.0，MPS 可用
- TG 发送：在 `/Users/zhangzc/fable-trading` 下调 `yoyo.notify.send_document`
  （凭据在 `fable-trading/data/tg_config.json`，不读取不回显 token）
- caption 是 HTML parse mode，`<` 要避开，否则 400
